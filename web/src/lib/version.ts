// Версия приложения — та же, что и в CHANGELOG.md и в ванильной версии
// (legacy/js/app.js, const APP_VERSION). Обновляется вручную при каждом
// наборе правок и показывается внизу сайдбара, чтобы по скриншоту было
// видно, какая сборка у пользователя.
export const APP_VERSION = "2.3.1"

// Папка автобэкапа по умолчанию — подставляется в настройки, чтобы не вбивать
// руками. Это подпись для глаз: доступ к папке всё равно даёт только браузер,
// по кнопке «Подтвердить доступ» (File System Access API не разрешает открыть
// путь без явного клика). Путь можно изменить прямо в настройках.
//
// Указывает внутрь Яндекс.Диска намеренно: два человека работают под разными
// аккаунтами на разных компьютерах, и им нужны бэкапы в одном месте. Синхронная
// папка решает это без единой строчки кода — каждая машина пишет свои файлы,
// диск сводит их вместе. Имена включают аккаунт и дату, поэтому машины никогда
// не пишут в один и тот же файл.
export const DEFAULT_BACKUP_PATH = "D:\\Yandex.Disk\\CRM-Бэкапы"
